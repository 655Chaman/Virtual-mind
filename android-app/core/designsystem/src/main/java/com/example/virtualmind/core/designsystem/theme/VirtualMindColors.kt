package com.example.virtualmind.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// Base Brand Colors
val VmParrotGreen = Color(0xFF00A86B)
val VmParrotGreenLight = Color(0xFF5DDBAA)
val VmParrotGreenDark = Color(0xFF007A4D)

// Gold Accents
val VmGold = Color(0xFFC9A84C)
val VmGoldDim = Color(0xFF7A6230)
val VmGoldBright = Color(0xFFF0C96A)

// Theme Surfaces (Dark Mode Focused)
val VmObsidian = Color(0xFF060606)
val VmSurface = Color(0xFF0E0E0E)
val VmSurface2 = Color(0xFF161616)
val VmTextDim = Color(0xFF6B6352)
val VmForeground = Color(0xFFEDEDED)

// Per-Pillar Colors
val PillarDeen = Color(0xFF10D86A)      // Sacred Jade Green
val PillarFitness = Color(0xFFF43F5E)   // Scarlet
val PillarWellness = Color(0xFF22D3EE)  // Glacier Cyan
val PillarElesium = Color(0xFF3B82F6)   // Sapphire Blue
val PillarSelf = Color(0xFFA855F7)      // Amethyst

val PillarQadr = Color(0xFF818CF8)      // Starlight Indigo
val PillarCommand = Color(0xFFF59E0B)   // Amber
val PillarOracle = Color(0xFF22C55E)    // Matrix Green

// System Colors
val SystemError = Color(0xFFC94C4C)     // Error / Penalty
